<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>Read and Review</title>
</head>
<body>
    <nav>
        <h4>ReadnReview</h4>
        <ul>
            <li>Bookshop</li>
            <li><a href="/books">BooksList</a></li>
            <li><a href="/">Logout</a></li>
        </ul>
    </nav>
    <div class="container mt-4">
        <div class="card" style="width: 18rem;">
            {{!-- <img src="..." class="card-img-top" alt="..."> --}}
            <div class="card-body">
                <h3 class="card-title">{{name}}</h3>
                <p class="card-text">{{bio}}</p>
                {{#if rating}}
                <h5>Average rating: {{rating}} stars</h5>
                {{else}}
                <h5>No ratings yet!</h5>
                {{/if}}
                <a href={{rating_link}} class="btn btn-primary">Add Rating</a>
                {{#if reviews}}
                    <h5>Reviews</h5>
                    <ul>
                        {{#each reviews}}
                            <li>{{username}}: {{review}}</li>
                        {{/each}}
                    </ul>
                {{else}}
                    <h5>No reviews yet!</h5>
                {{/if}}
                <a href={{review_link}} class="btn btn-primary">Add Review</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
</body>
</html>